# Agent Instructions

## Repository Identity

- Canonical repository: `ctg-tme/Joystick_CameraControl_ProductionSwitcher_using_Thrustmaster_16000M`.
- Canonical local checkout: `/Users/bomcgoni/Documents/GitHub/Joystick_CameraControl_ProductionSwitcher_using_Thrustmaster_16000M`.
- `Joystick_CameraControl_ProducionSwitcher_using_Thrustmaster_16000M` is the legacy misspelled name. Existing tasks may retain that working directory through a compatibility symlink, but new links, task configuration, and repository references must use the canonical name above.
- The separately versioned joystick dependency is the sibling repository `/Users/bomcgoni/Documents/GitHub/Thrustmaster_16000M-InputDevice-Class`. Inspect or change it only when the task explicitly includes the InputDevice class.
- Before repository work, confirm `git rev-parse --show-toplevel` and `git remote get-url origin` resolve to the canonical checkout and repository.

## Version Control

- Commit every change made in this repository so the work remains traceable.
- Do not push commits unless the user explicitly requests it.
